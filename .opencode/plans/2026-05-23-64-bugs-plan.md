# Complete Bugfix Plan — 64 bugs total

## Task 1: bot/keyboards.py (5 fixes)
1. **HIGH**: Fix garbled text `"Р"Р°Р»РµРµ в†'"` → `"Далее →"` in `build_exclude_keywords_keyboard`
2. **HIGH**: Fix city Back button — change `back_action=WizardAction.EXCLUDE_DONE` to handle `__back__` in `on_exclude_done`
3. **LOW**: Remove dead `build_keywords_keyboard` function
4. **LOW**: Add "Любой" option to EXPERIENCE dict for experience step
5. **LOW**: Remove unused `EXPERIENCE_DONE` enum

## Task 2: bot/handlers/filters.py (6 fixes)
1. **MEDIUM**: Re-render per-group keyboard in `on_keyword_toggle` using `_safe_edit`
2. **MEDIUM**: Add `current_group` to state in `on_keyword_group_select`
3. **MEDIUM**: Add Back/Cancel button to custom_salary step (trap fix)
4. **LOW**: Add toast feedback to `on_exclude_toggle` (`callback.answer("🚫 исключено")`)
5. **LOW**: Add state filter `FilterWizard.keywords` to `on_keyword_toggle`
6. **HIGH**: Fix city Back: add `__back__` branch in `on_exclude_done` handler

## Task 3: bot/handlers/control.py + card_actions.py (5 fixes)
1. **MEDIUM**: `on_check_now`: use `_safe_edit` instead of `message.answer()`, remove useless try/except
2. **LOW**: `on_vacancy_similar`: add `experience=None` to FSM data
3. **LOW**: `on_vacancy_similar`: avoid detached object access (move `words = ...` inside session)
4. **LOW**: `on_vacancy_block`: avoid nested sessions (pass session to add_blocklist or use existing)

## Task 4: core/scheduler.py (5 fixes)
1. **CRITICAL**: `vacancies = await scraper.search(keywords=search_terms, city=city)` — use expanded synonyms
2. **HIGH**: Fix salary range overlap check — use proper min/max intersection logic
3. **MEDIUM**: SSE event queue: use broadcast pattern (list of per-client queues)

## Task 5: core/database/models.py + repository.py (10 fixes)
1. **CRITICAL**: UNIQUE on `sent_vacancies(user_id, vacancy_id)`
2. **MEDIUM**: UNIQUE on `saved_vacancies(user_id, vacancy_id)`  
3. **MEDIUM**: UNIQUE on `blocklist(user_id, pattern, type)`
4. **MEDIUM**: CASCADE on all ForeignKeys
5. **MEDIUM**: Index on frequently queried columns
6. **MEDIUM**: Blocklist `created_at` timestamp
7. **LOW**: `DateTime(timezone=True)` on all timestamps
8. **CRITICAL**: `add_vacancy()` return existing instead of None
9. **HIGH**: `is_blocked()` limit SQL query instead of all-in-memory
10. **MEDIUM**: `get_or_create_user()` catch IntegrityError

## Task 6: web/app.py (6 fixes)
1. **HIGH**: `_get_first_user` — delete web_user after merge
2. **HIGH**: Add user scoping to filter/saved/blocklist endpoints
3. **MEDIUM**: `/api/filters/{id}/toggle` — return 404 if filter not found
4. **MEDIUM**: `/api/vacancies/{id}/block` — warn if company is empty

## Task 7: All 5 scrapers (10 fixes)
1. **MEDIUM**: trudvsem: wrap `int(smin)` in try/except (non-numeric salary crash)
2. **LOW**: trudvsem: ensure `source_id` is never empty string
3. **MEDIUM**: rabota.ru: change `range(1)` to `range(3)` for pagination
4. **MEDIUM**: rabota.ru: extract `published_at` from HTML
5. **LOW**: rabota.ru: fix experience detection — parse ranges like "1-3 года"
6. **LOW**: rabota.ru: fix URL typo `rabota.ru` → `www.rabota.ru`
7. **MEDIUM**: habr: extract `published_at` from HTML
8. **MEDIUM**: habr: extract experience from meta text "без опыта", "от 3 лет"
9. **MEDIUM**: habr: fix Удаленно in first meta position detection
10. **LOW**: utils/text_cleaner.py: single salary number → set only min OR max based on text

## Task 8: Frontend App.tsx, api/index.ts (5 fixes)
1. **HIGH**: Add `es.onerror` handler to SSE EventSource
2. **MEDIUM**: Memoize `handleLogin` with `useCallback`
3. **MEDIUM**: Fix `handleRefresh` to only fetch relevant data
4. **LOW**: Remove useless `setTimeout(0)` wrappers
5. **MEDIUM**: api/index.ts — merge headers properly (don't spread after)

## Task 9: Frontend ResultsPanel + VacancyCard + VacancyDetail (5 fixes)
1. **HIGH**: Replace `.toSorted()` with `[...arr].sort()` (2 locations)
2. **LOW**: Pass `onRefreshResults` to VacancyDetail for save/block refresh
3. **LOW**: VacancyDetail: add backdrop click to close
4. **LOW**: VacancyCard: add subtle hover shadow for clickability

## Task 10: Frontend FilterModal + FiltersPanel (4 fixes)
1. **MEDIUM**: Add custom salary input fields (free-form number inputs)
2. **MEDIUM**: Warn when same keyword selected in both include and exclude
3. **LOW**: FiltersPanel: show search input always (not just `>4`)
4. **LOW**: FiltersPanel: increase touch targets from 32px to 40px

## Task 11: Frontend History + Stats + ErrorBoundary + misc (4 fixes)
1. **MEDIUM**: HistoryPanel: fix sentinel cascade after manual refresh
2. **LOW**: HistoryPanel: show loading spinner during paginated fetches
3. **LOW**: StatsPanel: make chart colors reactive to theme changes
4. **MEDIUM**: ErrorBoundary: add `componentDidCatch` error logging

## Verification
- `pytest -v` — all tests pass
- `npm run build` — frontend builds
